server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Clean URLs: /app → app.html, /dashboard → dashboard.html, /agent → agent.html
    location = /app {
        try_files /app.html =404;
    }

    location = /dashboard {
        try_files /dashboard.html =404;
    }

    # Agent-readable setup page (production design, A/B parity with /vt220/agent)
    location = /agent {
        try_files /agent.html =404;
    }

    # API reference — redirect to the live Swagger UI on the API app
    location = /docs {
        return 302 https://central-intelligence-api.fly.dev/docs;
    }

    # VT220 A/B test — alternate landing + agent setup + API docs
    location = /vt220 {
        try_files /vt220.html =404;
    }

    location = /vt220/agent {
        try_files /vt220-agent.html =404;
    }

    location = /vt220/docs {
        try_files /vt220-docs.html =404;
    }

    location = /vt220/dashboard {
        try_files /vt220-dashboard.html =404;
    }

    # Legal pages: serve index.html and let the browser scroll to the anchor
    location = /privacy {
        rewrite ^ /#privacy redirect;
    }

    location = /terms {
        rewrite ^ /#terms redirect;
    }

    # Default: serve files normally
    location / {
        try_files $uri $uri/ =404;
    }
}
